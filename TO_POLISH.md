Things I noticed that require polishing:
1. text on tabs (home, brew, add, history) is a bit tool close to the bottom border
2. home page overall looks strange now. I would like to hide it for now, and try an app  without it. Lets make brew page the page which opens by default
3. when I enter a number, there is no easy way to hide keyboard, I have to click somewhere outside of it. I may click on another field, and then it is messy if the other field opens
4. also, when number/text field (something that requires keyboard) is on the bottom of the form, when I press it, they don't get pushed up (for example, in Add Grinder form, when I press Burr Size), as the result, I don't see what I am typing since it is hidden behind the keyboard and "Save Grinder" button
